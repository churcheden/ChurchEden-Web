# ChurchEden Web — copy to .env
# Vite only exposes variables prefixed with VITE_
# Domain values are the default; .env.development provides local fallback for npm run dev

VITE_APP_NAME=ChurchEden
VITE_APP_URL=https://churcheden.app
VITE_API_BASE_URL=https://api.churcheden.app/api/v1
